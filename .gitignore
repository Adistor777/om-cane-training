node_modules/
android/
www/
.DS_Store
.idea/
*.iml
*.log

# Throwaway demo-video test clips in the project root (not real content)
demo.mp4
demo-sound.mp4
*.mp4
.env
audio/

# Soundboard audio (bundled locally + synced to www, like audio/)
sounds/

# Children's photos bundled locally for demo profiles — NEVER in the repo
faces/

# Category setup photos (bundled locally + synced to www, like demo videos)
help-*.jpg

# Generated by scripts/a11y-preview.js — regenerate, don't commit.
docs/a11y-preview.html
# Artwork copied beside the preview so its relative ./img/ paths resolve.
# Source of truth is img/ at the repo root.
docs/img/

# Scratch explainer generated while reading app.js — regenerate, don't commit.
app-js-explained.html

# Meeting handoff packets: working material for a specific meeting, not source.
# Keep them locally; anything durable belongs in TRACKER.md or docs/.
docs/handoffs/
